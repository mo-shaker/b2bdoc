---
id: shipment-and-readiness
title: Step 3 — Shipment and Readiness
description: Create the shipment from eligible demand, read warehouse readiness, and stage the goods so the shipment becomes plannable.
sidebar_label: Step 3 — Shipment and Readiness
sidebar_position: 3
---

import DocumentInfo from '@site/src/components/DocumentInfo';
import Screenshot from '@site/src/components/Screenshot';

# Step 3 — Shipment and Readiness

<DocumentInfo
  category="Quick Start"
  audience={['Warehouse', 'Planning', 'Operations', 'UAT']}
  status="Review"
  version="1.0"
  owner="Product Team"
  updated="August 2026"
  readingTime="5 min"
/>

## Step 3a — The shipment forms itself

**Role**
Nobody. SCOP does this.

:::info[Shipment formation is automatic]

When a demand reaches **Eligible for Planning**, SCOP forms its shipment immediately — that is the blueprint's own formation flow, *Validated Demand → Allocate Inventory → Create Shipment*, and allocation is exactly what marking a demand eligible refreshes.

A second requirement on the **same route and the same required date** joins the shipment that is already open rather than forming another, which is how consolidation happens without anybody having to notice the opportunity. A shipment that has already been **planned** onto a trip is never joined: its capacity was sized when the plan was approved.

**Create Shipment** is still on the demand form as a re-run, for a demand made eligible before this release or one whose shipment was cancelled. Pressing it on a normal demand returns the shipment that already exists.

So **one demand does not necessarily equal one shipment**, and equally **one consolidated shipment does not create duplicate customer stops**: see the note below.

:::

**Navigate to**
`SCOP → Operations → Shipments`, or open the eligible demand and use its **Shipments** link.

**What to do**
Nothing. Check that the shipment is there, and go on to readiness.

**What SCOP does**
Groups the eligible demand into a transport requirement — a **Shipment** — with a total weight, volume, origin node, and destination node. It also creates the shipment's **Warehouse Readiness** record.

**Expected result**
A shipment exists in state **Ready**, with a readiness of **Conditionally Ready**.

**Verify**
`SCOP → Operations → Shipments`

<Screenshot
  src="quick-start/08-shipment-list.png"
  alt="SCOP Shipments list showing several shipments for the demo customer with their weight, volume, staged percentage and readiness."
  caption="One row per shipment, with its weight, volume and readiness. SHP-2026-000006 is the consolidated one — 172 kg across two demands, Conditionally Ready until it is staged. (The Staged % column currently renders a hundred times too large; see Release 1 Boundaries.)"
/>

---

## One shipment, one visit

Consolidation is about the **physical visit**, not the customer's name.

When compatible demands are consolidated into one shipment and represent the same physical customer visit, SCOP creates **one delivery stop** containing the combined work. Different branches, locations, dates, service windows, or activities remain separate stops.

The stop is one arrival, one service period, one departure, one proof of delivery — so two requirements only share it when arriving once can actually satisfy both. Two orders for the same commercial customer may still be two stops: a different branch is a different node, a pickup is not a delivery, and two different committed windows are two different promises that one arrival cannot keep.

:::info[Shared stop, separate requirements]

A shared stop does **not** merge the demands into one fulfilment record:

```text
One physical Stop
  → multiple delivery lines
    → each line still traceable to its own Demand and its own Odoo transfer
```

Actual quantities continue to come from the relevant Odoo transfers, so within a single visit `Demand A` can be fully delivered while `Demand B` is short and takes a backorder. One stop outcome does not force every demand into the same fulfilment state.

:::

Verified on `b2b`: two orders for the same branch and date produced `DMD-2026-000010` and `DMD-2026-000011`, one shipment `SHP-2026-000006`, and a trip with exactly two stops — the warehouse pickup and **one** customer delivery carrying a line for each demand.

---

## Step 3b — Read the readiness

**Role**
Warehouse

**Navigate to**
`SCOP → Operations → Warehouse Readiness`

Readiness answers one question — *can this shipment actually go, and why* — and it is **computed from evidence, never typed in**. It always carries its reasoning in words.

<Screenshot
  src="quick-start/11-warehouse-readiness-list.png"
  alt="SCOP Warehouse Readiness list showing one shipment with readiness Ready."
  caption="One row per shipment, with the readiness state and the node preparing the goods."
/>

<Screenshot
  src="quick-start/12-warehouse-readiness-form.png"
  alt="SCOP Warehouse Readiness form showing computed state, override state, evidence text, and the plannable flag."
  caption="The readiness record. Evidence explains the computation in plain words, so a planner asking 'why is this not ready' has an answer without reading source code."
/>

### The five readiness states

| Readiness | What it means | Plannable? |
| --- | --- | --- |
| **Ready** | Everything is staged | **Yes** |
| **Conditionally Ready** | Stock is reserved for the whole shipment but not yet staged, so it is expected to be available before execution | **Yes** |
| **Partially Ready** | Only part of the shipment can be covered | No |
| **Not Ready** | The goods are not prepared, or the shipment has no lines | No |
| **Blocked** | Something must be fixed first — unresolved ownership, or every transfer cancelled | No |

Only **Ready** and **Conditionally Ready** are plannable. `Conditionally Ready` counts because that is what the value *means*; excluding it would make the classification decorative.

`Blocked` is tested **first**, before any staged percentage. A shipment whose owner is unresolved is not "partially ready" — it is not plannable at all, and a well-prepared pallet must not be allowed to mask a legal problem.

### The supervisor override

A supervisor in the warehouse can see things the database cannot — a pallet already on the dock that Odoo has not caught up with. The **Override Readiness** field overrules the computation completely, and is recorded with who set it, when, and why. It is shown **beside** the computed value, never replacing it, so the computation stays auditable.

---

## Step 3c — Stage the goods

**Role**
Warehouse

**Navigate to**
`SCOP → Operations → Shipments` → open the shipment

**What to do**
Click **Stage Everything**.

Use it when the goods for the whole shipment are physically picked and standing in the staging area ready to load. It is a shortcut for confirming line-by-line — so use it only when that is actually true.

<Screenshot
  src="quick-start/09-shipment-form.png"
  alt="SCOP Shipment form showing the Refresh Readiness and Stage Everything buttons with readiness Conditionally Ready."
  caption="Before staging: readiness is Conditionally Ready, staged 0%. The header offers Refresh Readiness and Stage Everything."
/>

**What SCOP does**
Marks every shipment line staged, recomputes readiness, and moves the readiness state up.

**Expected result**
Readiness becomes **Ready**, with evidence reading *"Everything is staged."*

<Screenshot
  src="quick-start/10-shipment-staged.png"
  alt="SCOP Shipment form after staging, showing readiness Ready."
  caption="After Stage Everything: readiness Ready. The shipment is now firmly plannable."
/>

**Verify**
`SCOP → Operations → Warehouse Readiness` shows **Ready** for this shipment.

**If it does not work**

| Symptom | Check |
| --- | --- |
| Readiness stays **Blocked** | Ownership is unresolved on one or more lines — return to [Step 2](./demand-and-ownership.mdx) |
| Readiness stays **Not Ready** | The shipment has no lines, or nothing is reserved in Odoo |
| **Stage Everything** does nothing visible | Click **Refresh Readiness**; readiness is recomputed from current evidence |
| Staged quantity exceeds the shipment quantity | SCOP rejects it — you cannot stage more than the shipment asks for |

:::note

Staging is a **state**, not a separate document. You are recording that the goods are prepared, not creating a new record to track.

:::

## Next

[Step 4 — Generate the Plan](./generate-the-plan.mdx) — turn plannable shipments into a recommended daily plan.
