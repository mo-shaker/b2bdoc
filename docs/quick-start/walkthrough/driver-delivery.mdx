---
id: driver-delivery
title: Step 7 — Driver Delivery
description: The driver executes the trip, the warehouse validates the Odoo transfer, and the delivery is closed with proof of delivery.
sidebar_label: Step 7 — Driver Delivery
sidebar_position: 7
---

import DocumentInfo from '@site/src/components/DocumentInfo';
import Screenshot from '@site/src/components/Screenshot';

# Step 7 — Driver Delivery

<DocumentInfo
  category="Quick Start"
  audience={['Driver', 'Warehouse', 'Operations', 'UAT']}
  status="Review"
  version="1.0"
  owner="Product Team"
  updated="August 2026"
  readingTime="7 min"
/>

## The driver's sequence

```text
Trip → Depart → Stop → I have arrived → Start unloading → Delivered / Could not deliver
```

The driver's screens are a deliberately small surface: they see their own trips and their own stops, nothing else.

---

## Step 7a — Open the trip

**Role**
Driver

**Navigate to**
`SCOP → My Work → My Trips`

<Screenshot
  src="quick-start/25-driver-my-trips.png"
  alt="SCOP My Trips on a phone-sized screen, showing one trip card with its state."
  caption="My Trips on a phone. One card per trip, with its state and planned start."
  device="mobile"
/>

**What to do**
Tap the trip for today, then tap **Depart** when the vehicle leaves.

<Screenshot
  src="quick-start/26-driver-trip-ready.png"
  alt="SCOP driver trip form on a phone showing vehicle, planned start, and the two stops."
  caption="The driver's trip form — vehicle, planned start, and the stops in sequence. Deliberately minimal."
  device="mobile"
/>

**What SCOP does**
The trip moves to **In Progress**, and the shipments and demands it carries follow into execution. The driver performs one act; SCOP records its consequences.

:::info[The driver departs their own trip, and gains nothing else]

Departure is inside a driver's authority, and it is the only trip-level act they perform. Moving the shipments and demands into execution is not a second act the driver carries out — it is the system recording what departure means — so it runs on the system's behalf.

A driver still cannot open a demand, a shipment, a plan or the rule catalogue, and cannot see another driver's trip. That isolation is verified by automated tests, not by the absence of a button.

:::

---

## Step 7b — Arrive at the stop

**Navigate to**
`SCOP → My Work → My Stops`

<Screenshot
  src="quick-start/28-driver-my-stops.png"
  alt="SCOP My Stops on a phone showing the warehouse pickup stop and the SCOP Quick Start Demo Customer delivery stop."
  caption="My Stops, in trip and sequence order: stop 0 the warehouse pickup, stop 1 the SCOP Quick Start Demo Customer delivery."
  device="mobile"
/>

**What to do**
Open the customer stop. Tap **I have arrived** on arrival.

<Screenshot
  src="quick-start/29-driver-stop-planned.png"
  alt="SCOP driver stop form showing status Planned and the I have arrived button."
  caption="Stop status Planned. The only action offered is I have arrived — the screen never shows a button the driver should not press yet."
  device="mobile"
/>

**What SCOP does**
Records the **Actual Arrival** timestamp and moves the stop to **Arrived**. The difference between planned and actual arrival becomes the arrival-accuracy measure.

<Screenshot
  src="quick-start/30-driver-stop-arrived.png"
  alt="SCOP driver stop form with status Arrived and the actual arrival time recorded."
  caption="Arrived, with the actual arrival time stamped by the system rather than typed by the driver."
  device="mobile"
/>

**What to do next**
Tap **Start unloading** when unloading begins. The stop moves to **Servicing** and the outcome buttons appear.

<Screenshot
  src="quick-start/31-driver-stop-unloading.png"
  alt="SCOP driver stop form in Servicing status, offering Delivered and Could not deliver."
  caption="Servicing. Now — and only now — the two outcome buttons appear: Delivered and Could not deliver."
/>

:::note[On a phone, the buttons collapse into one menu]

At phone width the action buttons move into a single **Action** menu at the top of the form. The choices are the same.

:::

---

## Step 7c — Record proof of delivery

**Navigate to**
the **Proof of delivery** tab on the stop

<Screenshot
  src="quick-start/32-driver-proof-of-delivery.png"
  alt="SCOP driver stop Proof of delivery tab showing signed-by field, signature pad, photos and location."
  caption="Proof of delivery: who signed, their signature, photographs, and the delivery location."
/>

| Field | What it captures |
| --- | --- |
| **Signed By** | The name of the person who received the goods |
| **Signature** | Captured on the screen, tied to the name above |
| **Photos** | Any number of attachments — condition of goods, the dock, a damaged pallet |
| **Location** | The coordinates at the moment of delivery |

:::info[About the delivery location]

The coordinates are captured **once, at the point of delivery**. This is a proof-of-delivery stamp, not live vehicle tracking — SCOP does not follow the vehicle between stops in Release 1.

Coordinate capture needs an `https` deployment. On plain `http` the browser refuses, and SCOP records a **null with a reason** rather than silently storing zero — a stop at latitude 0 is in the Atlantic, and a plausible wrong answer is worse than an honest missing one.

:::

---

## Step 7d — Validate the Odoo transfer

This is the step that surprises people, and it is the heart of *Odoo drives, SCOP listens*.

**Role**
Warehouse

**Navigate to**
`Inventory → Transfers` → the delivery order for this order

<Screenshot
  src="quick-start/37-odoo-transfer-before-validate.png"
  alt="Odoo delivery order QSD/OUT/00001 in the Ready state with quantities set, before validation."
  caption="Before validation. The quantities are set but nothing is confirmed, so SCOP still reads the actual quantity as zero."
/>

**What to do**

1. Set the delivered quantity on each line. **Record a short delivery here** if the customer accepted less than planned.
2. Click **Validate**.

**Expected result**
The transfer reaches **Done**.

<Screenshot
  src="quick-start/38-odoo-transfer-validated.png"
  alt="Odoo delivery order QSD/OUT/00001 in the Done state after validation."
  caption="Done. The quantities are now facts, and SCOP can read them."
/>

**What SCOP does**
Reads the validated quantities back onto the stop's goods lines and onto the demand's fulfilled quantity. Verify it on the driver's **Goods** tab:

<Screenshot
  src="quick-start/33-driver-goods-quantities.png"
  alt="SCOP driver stop Goods tab showing planned quantity 12, 8, 20 and actual quantity 12, 8, 20, with a note that quantities come from the warehouse transfer."
  caption="Planned 24 / 15 / 30, actual 24 / 15 / 30 — read from the validated transfer. The note on the screen says it plainly: “Quantities come from the warehouse transfer. Record a short delivery there, not here.”"
/>

:::danger[Order matters — and SCOP tells the two cases apart]

If you press **Delivered** before the office has validated the transfer, the actual quantities are still zero. SCOP does **not** conclude that the delivery failed. It says so plainly:

> Stop 1 has no delivered quantity recorded yet. The warehouse validates the transfer in Odoo, and SCOP reads the result — wait for that, then close the stop. If the delivery genuinely failed, use 'Could not deliver' and give the reason.

<Screenshot
  src="quick-start/39-awaiting-odoo-validation.png"
  alt="Odoo warning telling the driver that no delivered quantity is recorded yet and that the warehouse validates the transfer in Odoo first."
  caption="Waiting on the office is not a failed delivery, and SCOP refuses to let it be recorded as one."
/>

This distinction matters. Asking a driver for a failure reason here would invite them to record a failure that never happened, and every service measure downstream would then have to be unpicked.

Once the transfer **is** validated and the quantities are genuinely short, a different message appears and `BR-EXE-001` asks for the reason:

> Stop 1 did not deliver everything planned, so it needs a reason before it can be closed.

:::

If a short delivery is validated in Odoo, Odoo may create a **backorder** for the remainder. Expect the original transfer to close at the delivered quantity and a new transfer to carry the rest. Because a backorder is another fulfilment of the *same* demand, SCOP does not create a second demand for it — and the demand's fulfilled quantity accumulates across both.

The demand rests at **Partially Completed** meanwhile. When the backorder is validated and the requirement is finally met, the same demand reaches **Completed**. A second short backorder leaves it exactly where it is, with the fulfilled quantity raised.

---

## Step 7e — Close the delivery

**What to do**
Back on the stop, tap **Delivered**.

**Expected result**
The stop reaches **Completed** with an **Outcome** of **Completed**, and no reason is required because nothing went wrong.

<Screenshot
  src="quick-start/36-driver-stop-delivered.png"
  alt="SCOP driver stop form showing status Completed and outcome Completed, with matching planned and actual quantities."
  caption="Completed. Outcome Completed, planned and actual quantities equal, proof of delivery attached."
/>

**Verify**

- The stop status is **Completed**.
- Once **every** stop on the trip has an outcome — including the warehouse pickup — the trip closes itself and reaches **Completed**. The driver has no Complete button, and does not need one.
- The shipment reaches **Completed**, and so does the demand, with its fulfilled quantity equal to what was requested.
- The delivery appears in the reports covered in [Reports You Should Know](../reports-you-should-know.mdx).

:::info[Close every stop, not only the customer's]

A trip refuses to complete while any stop has no outcome, and the warehouse pickup is a stop. If the trip is still **In Progress** after the customer has signed, the depot stop is the one still open.

:::

**If it does not work**

| Symptom | Check |
| --- | --- |
| **Delivered** says no quantity is recorded yet | The warehouse has not validated the Odoo transfer. Wait for it — do **not** record a failure that did not happen |
| **Delivered** asks for a reason | The transfer was validated short. Supply the reason for the shortfall |
| **Depart** is not offered | The trip is not **Ready to Depart**. Its loading tasks are still open — see [Step 6](./loading-and-departure.mdx) |
| The trip does not appear under **My Trips** | The trip's driver must be the `hr.employee` record linked to your login, and the trip must be Released or beyond |
| **Start unloading** is not offered | The stop is not **Arrived** yet |
| Actual quantities stay at zero | The transfer is not **Done**, or the moves are not linked to this demand |
| The driver sees no trips | The trip has no driver assigned, or the driver's user is not linked to their employee record |
| The trip is still **In Progress** after the last delivery | A stop still has no outcome — usually the warehouse pickup |
| The demand is still **In Execution** | The trip has not completed yet, or the transfer is short and a backorder is outstanding |

## Next

[Reports You Should Know](../reports-you-should-know.mdx), or read what happens when things go wrong in [When Things Do Not Go to Plan](../exceptions/unplanned-demand.mdx).
